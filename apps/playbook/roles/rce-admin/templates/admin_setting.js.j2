var config = {
    server: "http://{{ rce_admin_api }}", // 应用服务器接口地址
    product: {
        name: "{{ company_name }}", // 应用名称
        icon: "{{ company_icon }}",
        // 应用图标，可以使用网络地址，建议 icon 大小 32 x 32，私有部署改为 css/images/logo.png
        appName: "{{ company_en_name }}" //设置LDAP中用到，产品名称简称
    },
    upload: {
        type : "{{ file_server_type }}",   //七牛或私有云自带文件服务器 qiniu,私有云  RongCloud
        file:{
            fileSize:  100 * 1024 * 1024,  // 文件上限,私有云需要设置,默认最大100M
            domain: 'http://{{ file_server }}'   // 图片、文件 上传地址,私有云格式 'http://120.92.10.214:8080'
        },
        base64: {
            size: 5 * 1024 * 1024,   // base64大小, 私有云服务器需设置,默认5M
            domain: 'http://{{ file_server_base64 }}' // 上传 base64 图片资源,rcx: /upload/base64   私有云 /base64.php  公有云/七牛: http://upload.qiniu.com/putb64/-1
        }
    },
    email: {
        suffix:'rongcloud.cn',  //邮箱后缀
        address:'//workmail.rongcloud.cn/'      //邮箱登陆地址
    }
};
